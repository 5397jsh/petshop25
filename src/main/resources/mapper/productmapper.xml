<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.sm.repository.ProductRepository">

    <!-- 단일 상품 조회 -->
    <select id="select" parameterType="Integer" resultType="Product">
        SELECT *
        FROM product p
                 INNER JOIN cate c ON p.cate_id = c.cate_id
        WHERE p.product_id = #{productId}
    </select>

    <!-- 전체 상품 조회 -->
    <select id="selectAll" resultType="Product">
        SELECT *
        FROM product p
                 INNER JOIN cate c ON p.cate_id = c.cate_id
    </select>

    <!-- 상품 등록 -->
    <insert id="insert" parameterType="Product">
        INSERT INTO product
        (product_name, product_price, discount_rate, product_img, product_regdate, product_update, cate_id)
        VALUES
            (#{productName}, #{productPrice}, #{discountRate}, #{productImg}, NOW(), NOW(), #{cateId})
    </insert>

    <!-- 상품 수정 -->
    <update id="update" parameterType="Product">
        UPDATE product
        SET product_name = #{productName},
            product_price = #{productPrice},
            discount_rate = #{discountRate},
            product_img = #{productImg},
            product_update = NOW(),
            cate_id = #{cateId}
        WHERE product_id = #{productId}
    </update>


    <!-- 상품 삭제 -->
    <delete id="delete" parameterType="Integer">
        DELETE FROM product
        WHERE product_id = #{productId}
    </delete>
    <!-- 카테고리별 상품 담기 -->
    <select id="selectByCateId" parameterType="int" resultType="Product">
        SELECT
            p.product_id AS productId,
            p.product_name AS productName,
            p.product_price AS productPrice,
            p.discount_rate AS discountRate,
            p.product_img AS productImg,
            p.product_regdate AS productRegdate,
            p.product_update AS productUpdate,
            p.cate_id AS cateId,
            c.cate_down AS cateName
        FROM product p
                 LEFT JOIN cate c ON p.cate_id = c.cate_id
        WHERE p.cate_id = #{cateId}
    </select>

    <!-- 신상품 뭐시기 -->
    <select id="selectNewProducts" resultType="Product">
        SELECT *
        FROM product
        ORDER BY product_regdate DESC
            LIMIT 8
    </select>



</mapper>